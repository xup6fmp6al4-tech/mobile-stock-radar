services:
  - type: web
    name: mobile-stock-radar
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: MAX_CONCURRENT_SCRAPES
        value: "3"
